use crate::semantic::operators::OpTable;
use crate::semantic::primitives::Primitive;
use crate::semantic::{ConstValue, Node, NodeKind, Type};
use crate::{err::*, error};
use std::collections::HashSet;
