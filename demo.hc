let f = a => a + 1 in f

/* 
let Type :: {
	Ambiguous,
	TypeVariable with integer,
	Primitive with {
		Integer,
		Real,
		Boolean,
	},
	Product with List Type,
	Sum with List Type,
	Function with Type * Type,
	Type,
} in
Type.Product List.newkind integer
*/


