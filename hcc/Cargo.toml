[package]
name = "hcc"
version = "0.1.0"
edition = "2024"

[dependencies]
halcyon-lib = { path="../lib" }
clap = {version = "4.5", features=["derive"]}
wasmtime = {version = "34.0", default-features = false, features = ["runtime", "cranelift", "gc", "gc-drc"]}

